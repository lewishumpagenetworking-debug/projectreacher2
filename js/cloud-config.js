// Fill these in after creating a Supabase project (see supabase/schema.sql for setup steps).
// Project Settings -> API -> "Project URL" and "anon public" key.
//
// The anon key is safe to ship in client code by design — Supabase's Row
// Level Security policies (not secrecy of this key) are what protect data.
// Leaving these blank keeps the app running in local-only mode: everything
// works exactly as before, just without cloud sync.
export const SUPABASE_URL = "";
export const SUPABASE_ANON_KEY = "";
